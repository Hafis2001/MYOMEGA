import { Platform } from 'react-native';

/**
 * Global Configuration for the MYOMEGA App
 */
export const Config = {
    // DEVELOPMENT: Use local addresses
    // PRODUCTION: Change this to your hosted URL (e.g., 'https://myomegahrms.in/api/hr')
    API_BASE_URL: 'https://myomegahrms.in/api/hr',

    // Legacy or backup configuration if needed
    LEGACY_BASE_URL: 'https://myomegahrms.in/api/hr',
};
